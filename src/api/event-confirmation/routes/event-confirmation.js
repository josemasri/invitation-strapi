'use strict';

/**
 * event-confirmation router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::event-confirmation.event-confirmation');
