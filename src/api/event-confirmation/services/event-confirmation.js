'use strict';

/**
 * event-confirmation service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::event-confirmation.event-confirmation');